# Odoo Watcher Configuration

# Odoo Connection Settings
ODOO_URL=http://localhost:8069
ODOO_DB=odoo
ODOO_USERNAME=admin
ODOO_API_KEY=fc8d60e57586da18c580f6dab7db48f4df0b40ae

# Odoo MCP Server Settings
ODOO_MCP_URL=http://localhost:8000

# Watcher Settings
CHECK_INTERVAL=300  # Check every 5 minutes (300 seconds)
BATCH_SIZE=50       # Number of records to fetch per check

# Monitoring Settings
MONITOR_INVOICES=true
MONITOR_PAYMENTS=true
MONITOR_BILLS=true
MONITOR_PARTNERS=false

# Email Notification Settings
NOTIFICATION_ENABLED=true
NOTIFICATION_EMAIL=your-email@example.com
NOTIFICATION_THRESHOLD=0  # Notify for all invoices (set to 1000 to only notify for invoices >= $1000)

# State Tracking
STATE_FILE=Logs/odoo_watcher_state.json

# Output Settings
CREATE_TASKS=true
SAVE_TO_ACCOUNTING=true

# Paths (relative to vault root)
NEEDS_ACTION_PATH=Needs_Action
ACCOUNTING_PATH=Accounting
LOG_PATH=Logs
